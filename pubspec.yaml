name: seatlect_mobile
description: User mobile client for Seatlect system.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  bloc: ^6.1.0
  flutter_bloc: ^6.1.0
  equatable: ^1.2.5
  formz: ^0.3.2
  google_fonts: ^1.1.1
  entity:
    path: packages/entity

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
