name: token_manager
description: Handles token and configure middleware layer

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  grpc: ^2.8.0
  meta: ^1.2.4
  bloc: ^6.1.0
